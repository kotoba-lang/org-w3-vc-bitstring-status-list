{
  "private": true,
  "description": "npm dependencies of the ClojureScript half (org-nist-sha2 cascade; no direct @noble/hashes).",
  "dependencies": {},
  "scripts": {
    "smoke": "nbb --classpath \"$(clojure -Spath | tr ':' '\\n' | grep -E '(^src$|gitlibs)' | tr '\\n' ':')\" test/nbb_smoke.cljk"
  }
}
